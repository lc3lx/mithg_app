# Dating App Backend API - تطبيق تعارف 💕

تطبيق تعارف شامل مبني بـ Node.js و Express و MongoDB مع مميزات متقدمة للتعارف والتواصل الاجتماعي.

## 🚀 المميزات الرئيسية

### 👤 إدارة المستخدمين

- **تسجيل وتسجيل دخول آمن** مع JWT
- **ملفات شخصية شاملة** (الصور، السيرة الذاتية، التفضيلات)
- **التحقق من رقم الهاتف** للأمان
- **إدارة الخصوصية** (حظر المستخدمين)

### 💬 الدردشة والتواصل

- **دردشة فورية** بين المستخدمين
- **إرسال الصور والرسائل النصية**
- **إشعارات فورية** للرسائل الجديدة
- **حالة القراءة** للرسائل

### 👫 نظام الأصدقاء والطلبات

- **إرسال طلبات الصداقة**
- **قبول/رفض الطلبات**
- **إدارة قائمة الأصدقاء**
- **منع المستخدمين**

### 📱 المعرض والملف الشخصي

- **معرض صور وفيديوهات شخصية**
- **قسم "عني" للنبذة الشخصية**
- **إدارة المحتوى الشخصي**

### 📰 المنشورات الإدارية

- **منشورات من قبل الإدارة فقط**
- **أخبار وتحديثات التطبيق**
- **الإعجاب والتفاعل مع المنشورات**

### 🎯 نظام المطابقة الذكي

- **خوارزمية مطابقة متقدمة** بناءً على:
  - التفضيلات العمرية والجنسية
  - الموقع الجغرافي
  - الاهتمامات المشتركة
  - مستوى النشاط
- **عرض المطابقين المحتملين**
- **نظام الإعجاب بالملفات الشخصية**

### 🔔 نظام الإشعارات

- **إشعارات فورية** للأحداث المختلفة
- **أنواع متعددة من الإشعارات**
- **إدارة حالة القراءة**

## 🛠️ التقنيات المستخدمة

- **Node.js** - بيئة التشغيل
- **Express.js** - إطار العمل
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ODM لـ MongoDB
- **JWT** - المصادقة والتحقق
- **Bcrypt** - تشفير كلمات المرور
- **Sharp** - معالجة الصور
- **Multer** - رفع الملفات
- **Express-validator** - التحقق من البيانات

## 📁 هيكل المشروع

```
backend/
├── config/
│   ├── database.js          # إعداد قاعدة البيانات
│   └── config.env           # المتغيرات البيئية
├── models/                  # نماذج البيانات
├── routes/                  # المسارات
├── services/                # خدمات العمل
├── middlewares/             # الوسائط المتوسطة
├── utils/                   # الأدوات المساعدة
├── uploads/                 # الملفات المرفوعة
└── server.js                # نقطة البداية
```

## 🚀 التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone [repository-url]
cd backend
```

### 2. تثبيت التبعيات

```bash
npm install
```

### 3. إعداد قاعدة البيانات

تأكد من تشغيل MongoDB على المنفذ الافتراضي (27017)

### 4. إعداد المتغيرات البيئية

قم بإنشاء ملف `config.env` وأضف:

```env
DB_URI=mongodb://localhost:27017/dating_app
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=90d
BASE_URL=http://localhost:8000
NODE_ENV=development
```

### 5. تشغيل التطبيق

```bash
npm run start:dev
```

التطبيق سيعمل على: `http://localhost:8000`

## 📚 API Endpoints

### 🔐 المصادقة

- `POST /api/v1/auth/signup` - تسجيل مستخدم جديد
- `POST /api/v1/auth/login` - تسجيل الدخول

### 👤 المستخدمين

- `GET /api/v1/users/getMe` - الحصول على بياناتي
- `PUT /api/v1/users/updateMe` - تحديث بياناتي

### 📱 الملف الشخصي

- `GET /api/v1/profile/:userId/profile` - عرض الملف الشخصي الكامل
- `GET /api/v1/profile/:userId/gallery` - عرض معرض الصور
- `PUT /api/v1/profile/about` - تحديث قسم "عني"
- `POST /api/v1/profile/gallery` - إضافة صورة للمعرض

### 📰 المنشورات الإدارية

- `GET /api/v1/posts` - الحصول على المنشورات الإدارية
- `POST /api/v1/posts` - إنشاء منشور إداري (للأدمن فقط)
- `POST /api/v1/posts/:id/like` - الإعجاب بمنشور

### 💬 الدردشة

- `GET /api/v1/chats` - قائمة الدردشات
- `POST /api/v1/chats` - إنشاء دردشة جديدة
- `POST /api/v1/chats/:id/messages` - إرسال رسالة

### 👫 الأصدقاء

- `GET /api/v1/friend-requests` - طلبات الصداقة
- `POST /api/v1/friend-requests` - إرسال طلب صداقة
- `GET /api/v1/friend-requests/friends/list` - قائمة الأصدقاء

### 🎯 المطابقة

- `GET /api/v1/matches` - المطابقون المحتملون
- `GET /api/v1/matches/:userId` - ملف شخصي مفصل

### 🔔 الإشعارات

- `GET /api/v1/notifications` - الإشعارات
- `PUT /api/v1/notifications/:id/read` - تحديد كمقروء

---

## 🧪 الاختبار

```bash
# تشغيل جميع الاختبارات
npm test

# تشغيل الاختبارات مع التغطية
npm run test:coverage

# تشغيل الاختبارات في وضع المراقبة
npm run test:watch

# إضافة بيانات تجريبية لقاعدة البيانات
npm run seed

# إضافة بيانات تجريبية للاختبارات
npm run seed:test
```

### 📊 تغطية الاختبارات

- **Authentication API**: تسجيل الدخول، التسجيل، إعادة تعيين كلمة المرور
- **User Profile API**: إدارة الملف الشخصي والمعرض
- **Matching API**: خوارزمية المطابقة والتفاعلات
- **Performance Tests**: اختبارات الأداء والتحميل
- **Security Tests**: اختبارات الأمان و rate limiting

**معايير التغطية المستهدفة:**

- Statements: > 80%
- Branches: > 75%
- Functions: > 85%
- Lines: > 80%

---

**تم تطوير هذا المشروع كتطبيق تعارف شامل مع جميع المميزات الأساسية والمتقدمة.**
